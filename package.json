{
  "name": "music-vue2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=8081 public/",
    "mock": "json-server --watch mock/db.json --port 3000"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "json-server": "^0.17.0",
    "mockjs": "^1.1.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
